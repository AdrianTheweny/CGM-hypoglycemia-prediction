# CGM-hypoglycemia-prediction
Machine learning project to predict hypoglycemia events from continuous glucose monitor (CGM) time-series data. This work is a simplified reimplementation of Fleischer et al. (2022), using engineered temporal features and ensemble models to provide early warning predictions.
